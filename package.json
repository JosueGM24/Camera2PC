{
  "name": "camera2pc",
  "version": "1.0.0",
  "private": true,
  "description": "La camara del telefono o tablet en la PC, via WebRTC, y de ahi a Teams/Zoom con OBS",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-config.mjs",
    "dev": "node scripts/build-config.mjs && npx --yes netlify-cli dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.11"
  }
}
